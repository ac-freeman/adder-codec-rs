[package]
name = "adder-viz"
version = "0.2.4"
edition = "2021"
resolver = "2"
authors = ["Andrew C. Freeman"]
description = """
A GUI project to visualize and tune parameters of an ADΔER asynchronous video transcode.
"""
homepage = "https://github.com/ac-freeman/adder-codec-rs/wiki"
repository = "https://github.com/ac-freeman/adder-codec-rs/tree/main/adder-viz"
readme = "README.md"
license = "MIT OR Apache-2.0"
keywords = ["neuromorphic", "visualization", "video", "asynchronous", "event"]
categories = ["multimedia::encoding", "multimedia::video", "science"]
default-run = "adder-viz"

[features]
feature-logging = ["adder-codec-rs/feature-logging"]
feature-logging-nonmaxsuppression = ["adder-codec-rs/feature-logging-nonmaxsuppression"]

[dependencies]
adder-codec-core = { version = "0.2.1", path = "../adder-codec-core" }
adder-codec-rs = { version = "0.3.4", path = "../adder-codec-rs", features = [
    "transcoder",
] }
bevy = "0.11"
bevy_egui = "0.21.0"
crossbeam-channel = "0.5.6"
egui_file = "0.11.0"
futures = "0.3.26"
rayon = "1.5.3"
rfd = "0.10.0"
strum = "0.24.1"
strum_macros = "0.24.3"
tokio = { version = "1.20.1", features = ["full"] }
ndarray = "0.15.6"

[dependencies.opencv]
version = "0.84.5"
package = 'opencv'
default-features = false
features = ['videoio', 'imgproc', 'highgui', 'clang-runtime']
optional = false
